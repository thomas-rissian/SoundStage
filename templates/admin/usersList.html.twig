{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste utilisateur : </h1>
    <ul>
        {% for user in users %}

            <li><a href="{{ path('app_admin_user_edit', {id: user.id}) }}">{{ user.email }}</a></li>

        {% endfor %}

    </ul>


{% endblock %}

{% block title %}

{% endblock %}

